[build]
builder = "nixpacks"
buildCommand = "cd frontend && npm install --include=dev && NODE_OPTIONS=--max-old-space-size=2048 npm run build && cd ../backend && npm install --omit=dev"

[deploy]
startCommand = "node backend/server.js"
healthcheckPath = "/api/health"
